(function ($) {
    'use strict';

    $(document).ready(function(){


        if ($('.register-supplier-company').length > 0) {
            console.log('register-supplier-company');

        }

        // register-supplier-company
        // $('#intersectionCells select').each(function(){
        //     var $this = $(this);
        //     if($this.val() === 'Select Cell'){
        //         var error = 'Please select a cell' ; // take the input field from label
        //         $this.next('span').text(error);
        //         errorCount = errorCount + 1;
        //     }
        // });

    });

})( jQuery );